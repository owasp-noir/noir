apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: api
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`ing.example.com`) && PathPrefix(`/ing`) && Method(`POST`)
      kind: Rule
      services:
        - name: whoami
          port: 80
