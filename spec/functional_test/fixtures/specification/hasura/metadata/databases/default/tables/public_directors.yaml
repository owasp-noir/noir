table:
  name: directors
  schema: public
array_relationships:
  - name: movies
    using:
      foreign_key_constraint_on:
        column: director_id
        table:
          name: movies
          schema: public
select_permissions:
  - role: public
    permission:
      columns:
        - name
      filter: {}
