[package]
name = "rust-security-axum-fixture"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7"
tower-http = { version = "0.6", features = ["cors", "set-header", "limit"] }
tower_governor = "0.4"
http = "1"
