[package]
name = "axum-callees-example"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = { path = "../../axum" }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
