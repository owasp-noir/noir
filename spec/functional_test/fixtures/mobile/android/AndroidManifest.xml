<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapp">

    <application android:label="MyApp">

        <!-- Launcher activity: no <data>, must NOT produce any endpoint.
             The <nav-graph> reference is resolved by globbing
             res/navigation/*.xml, not by following this element. -->
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <nav-graph android:resource="@navigation/nav_graph" />
        </activity>

        <!-- gradle manifest placeholders: component name from
             ${applicationId}, scheme/host from manifestPlaceholders -->
        <activity android:name="${applicationId}.PlaceholderActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="${deepLinkScheme}" android:host="${deepLinkHost}" />
            </intent-filter>
        </activity>

        <!-- Placeholder missing from build.gradle: kept verbatim and
             tagged "unresolved" -->
        <activity android:name=".UnresolvedActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="myapp" android:host="${missingHost}" />
            </intent-filter>
        </activity>

        <!-- Custom scheme deep link with templated path -->
        <activity android:name=".DeepLinkActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="myapp" android:host="complex" android:path="/{id}" />
            </intent-filter>
        </activity>

        <!-- Custom scheme deep link using pathPrefix -->
        <activity android:name=".AccountActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="myapp" android:host="accounts" android:pathPrefix="/profile" />
            </intent-filter>
        </activity>

        <!-- Scheme resolved from @string/ reference -->
        <activity android:name=".SettingsActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/deep_link_scheme" android:host="settings" />
            </intent-filter>
        </activity>

        <!-- Verified App Link over https (universal-link) -->
        <activity android:name=".WebActivity" android:exported="true">
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="myapp.example.com" android:path="/complex/{id}" />
            </intent-filter>
        </activity>

        <!-- Exported component with a data-less custom action (IPC surface) -->
        <service android:name=".SyncService" android:exported="true">
            <intent-filter>
                <action android:name="com.example.ACTION_SYNC" />
            </intent-filter>
        </service>

    </application>
</manifest>
